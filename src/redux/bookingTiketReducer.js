const initialState = {

}

const bookingTicketReducer = (state = initialState, action) => {
    switch (action.type) {
        case '':



        default:
            return { ...state }
    }

}

export default bookingTicketReducer